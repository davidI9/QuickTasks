# QuickTasks Hyprland integration snippet
# Add this to your Hyprland config to launch the widget with a keybind.
# Requires install.sh has been run and task-calendar-launch is available in ~/.local/bin

bind = MOD+SHIFT+T, exec, ~/.local/bin/task-calendar-launch
